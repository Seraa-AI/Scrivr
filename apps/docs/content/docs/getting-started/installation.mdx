---
title: Installation
description: Install Inscribe into your project and configure TypeScript.
---

## Prerequisites

- **Node.js** ≥ 18 (LTS recommended)
- A package manager — `pnpm`, `npm`, or `yarn`

<Callout type="info">
  Inscribe is written in strict TypeScript. You don't need to install separate `@types/*` packages — type declarations are bundled with each package.
</Callout>

---

## Install the Packages

### React

<Tabs items={["pnpm", "npm", "yarn"]}>
  <Tab value="pnpm">
    ```bash
    pnpm add @inscribe/core @inscribe/react
    ```
  </Tab>
  <Tab value="npm">
    ```bash
    npm install @inscribe/core @inscribe/react
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @inscribe/core @inscribe/react
    ```
  </Tab>
</Tabs>

### Headless (no framework)

`@inscribe/core` has no framework dependency and can be used in any JavaScript environment — Node.js, vanilla JS, or as a base for a custom framework adapter.

<Tabs items={["pnpm", "npm", "yarn"]}>
  <Tab value="pnpm">
    ```bash
    pnpm add @inscribe/core
    ```
  </Tab>
  <Tab value="npm">
    ```bash
    npm install @inscribe/core
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @inscribe/core
    ```
  </Tab>
</Tabs>

### Optional Packages

| Package | Purpose |
| --- | --- |
| `@inscribe/plugins` | **Real-time collaboration**, **AI Toolkit**, and **Track Changes**. |
| `@inscribe/export` | Export to paginated **PDF** and **Markdown**. DOCX coming soon. |

---

## Setting Up Optional Features

### Real-Time Collaboration

The collaboration layer syncs over WebSockets using **Yjs** CRDTs. You will need to run your own WebSocket backend — any [Hocuspocus](https://hocuspocus.dev/)-compatible server works. Install `@inscribe/plugins` on the frontend:

<Tabs items={["pnpm", "npm", "yarn"]}>
  <Tab value="pnpm">
    ```bash
    pnpm add @inscribe/plugins
    ```
  </Tab>
  <Tab value="npm">
    ```bash
    npm install @inscribe/plugins
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @inscribe/plugins
    ```
  </Tab>
</Tabs>

Set the WebSocket server URL so the frontend can connect. With Vite:

```bash
VITE_WS_URL=ws://localhost:1234
```

The variable name depends on your bundler — Vite requires the `VITE_` prefix, Next.js uses `NEXT_PUBLIC_`, etc.

See the [Collaboration example](/examples/collaboration) for the full setup.

---

### AI Toolkit

`AiToolkit` is bring-your-own-LLM — it provides the editor-side context and ghost text overlay, but the actual LLM call is made from your own backend endpoint. Install `@inscribe/plugins` on the frontend and whatever AI SDK you prefer on the backend:

<Tabs items={["pnpm", "npm", "yarn"]}>
  <Tab value="pnpm">
    ```bash
    # Frontend
    pnpm add @inscribe/plugins

    # Backend (example using Vercel AI SDK + Anthropic)
    pnpm add ai @ai-sdk/anthropic
    ```
  </Tab>
  <Tab value="npm">
    ```bash
    # Frontend
    npm install @inscribe/plugins

    # Backend (example using Vercel AI SDK + Anthropic)
    npm install ai @ai-sdk/anthropic
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    # Frontend
    yarn add @inscribe/plugins

    # Backend (example using Vercel AI SDK + Anthropic)
    yarn add ai @ai-sdk/anthropic
    ```
  </Tab>
</Tabs>

**Environment variable** (backend only, if using Anthropic):

```bash
ANTHROPIC_API_KEY=your-key-here
```

See the [AI Assistant example](/examples/ai-assistant) for the full integration pattern.

---

## Bundler Support

Inscribe has been tested with **Vite** (recommended), **Webpack 5**, and **esbuild**. No special plugins or aliases are required — the packages ship pre-bundled ESM.

---

## Next Steps

<Cards>
  <Card title="Quick Start" href="/getting-started/quick-start">
    Build your first editor in under 30 lines of React.
  </Card>
  <Card title="Editor Options" href="/getting-started/editor-options">
    Full reference for all configuration options.
  </Card>
</Cards>
