---
---

No release. Test-only: the mark-rendering baseline now orders marks the way
ProseMirror does — schema rank — instead of an order no document can produce.
An order-sensitive change to mark rendering could previously move real output
while the baseline stayed green.
