---
---

No release. Documentation only: brings the font-resolution RFC's "What shipped"
section up to date with the work that landed after its three phases, corrects a
claim about `fontTable.xml` that the file itself disproves, and removes
recommendations of `.woff2` — a format an export now refuses — from the
typography guide, the roadmap and `CLAUDE.md`.
